/** @type {import('next').NextConfig} */
const nextConfig = {
  // devIndicators: false,
  output: "export",
  images: {
    unoptimized: true,
  },
  basePath: "/table-manners",
};

export default nextConfig;
